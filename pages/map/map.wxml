<!--pages/map/map.wxml-->
<view class="container">
  <map  style="width:100%;height: 1200px;" latitude="{{latitude}}" longitude="{{longitude}}" scale="{{scale}}" markers='{{markers}}' bindmarkertap="onMarkerTap"/>
  <detail markerInfo="{{markerInfo}}"></detail>
</view>